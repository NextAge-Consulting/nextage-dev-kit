One file per pattern, named for the pattern. Lead with a one-line summary.

This directory ships empty from the kit — references are earned by research in
the project that needs them, and are never overwritten by a sync.
